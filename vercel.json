{
  "version": 2,
  "builds": [
    {
      "src": "src/agent/index.ts",
      "use": "@vercel/node",
      "config": { 
        "installCommand": "apt-get update && apt-get install -y build-essential libcairo2-dev libpango1.0-dev libjpeg-dev libgif-dev librsvg2-dev pkg-config"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/agent/index.ts"
    }
  ]
} 